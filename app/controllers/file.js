app.controller('FileCtrl',
	function($scope, FileFactory) {
	/*
    $scope.file = FileFactory.all().then(function(file) {
        $scope.file = file;
    },function(msg){
        alert(msg);
    });
	*/
	}
);